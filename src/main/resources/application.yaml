server:
  port: 8088

spring:
  web:
    resources:
      # In Docker the static dir is mounted at /app/static; falls back to classpath for direct runs
      static-locations: file:/app/static/,classpath:/static/

turn:
  uri: "turn:localhost:3478"
  username: "javartc"
  credential: "javartc"
